# define functions, using tuples to return multiple values
def add_and_sub(a,b):
    """
    returns the sum and difference as (sum, difference)
    """
    mysum = a + b
    mydiff = a - b
    return(mysum, mydiff)